<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent" android:layout_height="match_parent"><LinearLayout
    android:id="@+id/layoutLineas"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mapa General del Metro"
        android:textSize="20sp"
        android:textStyle="bold"
        android:paddingBottom="12dp"/>

    <ImageView
        android:id="@+id/mapaGeneral"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/mapa_general_metro"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:contentDescription="Mapa general del metro"
        android:layout_marginBottom="24dp"/>

    <!-- Aquí se añadirán dinámicamente los botones para las líneas 1-12 desde Java -->

</LinearLayout>
</ScrollView>
